# Test Matrix

Purpose: map framework areas to existing test coverage and identify common gaps quickly.

## Coverage Map

| Area | Primary tests | What is covered | Typical gap to watch |
| --- | --- | --- | --- |
| Element lifecycle and reconciliation | `src/Flutter.Tests/ElementLifecycleTests.cs` | Keyed reorder, global key reparent/activation/disposal behavior, mixed keyed/unkeyed sibling reorder, and nested multi-parent mixed keyed/unkeyed reorder (keyed retention + moved unkeyed disposal). | Deep stress cases with rapid multi-frame updates that combine nested mixed keyed/unkeyed groups and `forgottenChildren` paths. |
| Inherited dependencies | `src/Flutter.Tests/InheritedWidgetTests.cs`, `src/Flutter.Tests/InheritedModelTests.cs`, `src/Flutter.Tests/InheritedNotifierTests.cs` | Dependent registration and selective rebuild semantics. | Edge cases around deep tree shadowing plus dynamic notifier swaps. |
| Frame and scheduler flow | `src/Flutter.Tests/FramePipelineTests.cs` | Begin/draw/post-frame order, persistent callbacks, build scheduling in frame. | Long-running callback interactions with host-level visibility transitions. |
| Render object parity | `src/Flutter.Tests/RenderingParityTests.cs` | Constraint normalization, relayout boundary behavior, root relayout triggers, failed-layout exception propagation with dirty-state preservation, `BoxConstraints.Tighten` clamp behavior for out-of-range requests, no-op invalidation guards in proxy/flex property setters, and overflow debug paint regression coverage for `RenderFlex` (overflow adds extra indicator paint commands versus non-overflow layout). | Rare constraint edge cases across deep mixed proxy/flex chains. |
| Compositing and layers | `src/Flutter.Tests/CompositingLayerTests.cs`, `src/Flutter.Tests/LayerV2Tests.cs` | Repaint boundary behavior, layer reuse/property updates, layer tree structure, no-op `RenderColoredBox` color updates, no-op `ReplaceRootLayer` handling, combined repaint + composited-layer update correctness in a single frame, and resize-driven clip-layer bounds refresh for implicit `RenderClipRRect` clipping. | Layer churn under repeated boundary toggles in large trees. |
| Basic proxy/layout/decoration widgets | `src/Flutter.Tests/BasicWidgetProxyTests.cs`, `src/Flutter.Tests/AlignTests.cs`, `src/Flutter.Tests/AspectRatioTests.cs`, `src/Flutter.Tests/FractionallySizedBoxTests.cs`, `src/Flutter.Tests/FittedBoxTests.cs`, `src/Flutter.Tests/UnconstrainedLimitedBoxTests.cs`, `src/Flutter.Tests/OverflowBoxTests.cs`, `src/Flutter.Tests/OffstageTests.cs`, `src/Flutter.Tests/StackTests.cs`, `src/Flutter.Tests/DecoratedBoxTests.cs`, `src/Flutter.Tests/ContainerTests.cs` | Widget-to-render wiring and rebuild update behavior for `Opacity`, `Transform`, `ClipRect`, `Align/Center`, `AspectRatio`, `Spacer`, `FractionallySizedBox`, `FittedBox`, `UnconstrainedBox/LimitedBox`, `OverflowBox/SizedOverflowBox`, `Offstage`, `Stack/Positioned`, `DecoratedBox`, and `Container` composition wrappers (`alignment`, `padding`, `margin`, `constraints`, `transform`), including alignment offsets, shrink-wrap factor behavior, ratio layout resolution on bounded axes, `Spacer` flex parent-data propagation, two-spacer proportional flex distribution in `RenderFlex`, fractional tight-constraint propagation with pass-through axes, `BoxFit`-driven fitted sizing/transform hit testing, axis-specific unconstraining with unbounded-axis `LimitedBox` max-clamp behavior, optional overflow constraint overrides with `OverflowBoxFit` sizing modes, fixed-size overflow alignment, offstage collapse semantics (layout without paint/hit-test and smallest parent size), positioned insets resolution, decoration data propagation (`border/radius/fill`), width/height tightening vs. explicit constraints, and wrapper ordering. | Richer nested layout stress scenarios with deep overlay stacks and mixed unconstrained parents. |
| Text widget and paragraph layout parity | `src/Flutter.Tests/TextWidgetTests.cs` | `Text` widget -> `RenderParagraph` wiring for layout-affecting options (`textAlign`, `softWrap`, `maxLines`, `overflow`, `textDirection`, `fontWeight`, `fontStyle`, `height`, `letterSpacing`), inherited `DefaultTextStyle` resolution/override behavior, plus unbounded-layout regression coverage to ensure paragraph width is not artificially capped. | Runtime visual parity across hosts/fonts (line metrics and glyph shaping) still needs screenshot/manual validation. |
| Material theming, shell, and button baseline | `src/Flutter.Tests/TextWidgetTests.cs`, `src/Flutter.Tests/MaterialScaffoldTests.cs`, `src/Flutter.Tests/MaterialButtonsTests.cs` | `Theme(data: ThemeData)` inherited lookup and `ThemeData.TextTheme.BodyMedium` propagation through `DefaultTextStyle` into framework `Text`/`RenderParagraph` defaults, `Scaffold` background resolution, app-bar background/foreground color precedence (`widget -> appBarTheme -> theme defaults`), app-bar icon-theme precedence for leading/actions slots (`iconTheme`: `widget -> appBarTheme -> foreground fallback`; `actionsIconTheme`: `widget -> appBarTheme -> iconTheme -> foreground fallback`, including actions fallback to widget `iconTheme` and foreground fallback when icon theme color is unset), app-bar leading-width precedence (`widget -> appBarTheme -> default`) with non-positive themed-width guard, plus leading-slot tight geometry by resolved leading width and toolbar height, app-bar toolbar-height precedence (`widget -> appBarTheme -> default`) with non-positive themed-height guard, app-bar actions-padding precedence (`widget -> appBarTheme -> default zero`), app-bar toolbar geometry parity (`AppBar` outer toolbar padding defaults to `0`, actions row applies no extra hardcoded inter-item spacing, actions row uses `mainAxisSize: min`, and actions row cross-axis alignment follows `ThemeData.UseMaterial3` mode: `center` for M3 and `stretch` for M2), app-bar title-layout parity coverage (`centerTitle` precedence across widget/theme/platform, macOS action-count platform fallback, `titleSpacing` horizontal insets, negative spacing guard, widget-vs-theme precedence for `titleSpacing`, and default `titleText` single-line ellipsis behavior (`softWrap: false`, `maxLines: 1`, `overflow: ellipsis`), including empty-string `titleText` rendering as text instead of being treated as missing title), app-bar text-style parity coverage (`titleTextStyle`/`toolbarTextStyle` resolution precedence widget -> theme -> defaults, title fallback through `TextTheme.TitleLarge`, and inherited text-style application for title/actions, including mixed actions-subtree context where toolbar text style and actions icon theme are inherited together), first Material button set defaults/disabled visuals (`TextButton`, `ElevatedButton`, `OutlinedButton`), pointer pressed-state visual transition coverage for `ElevatedButton`, hover-state enter/exit overlay coverage for `TextButton`, pointer-click focus-overlay reset coverage for `TextButton`, combined-state overlay priority coverage for Material buttons (`pressed+hovered`, `pressed+focused`, and `focused+hovered` conflict cases), keyboard-activation coverage (`Space` uses pressed overlay and invokes callback on key-down only, `NumPadEnter` activates, modified activation chords are ignored), ink-splash activation coverage for pointer down (`RenderInkSplash` presence/state), tight-width splash bounds coverage (`RenderInkSplash` width matches button width), rounded-splash clipping coverage through `ClipRRect` integration, baseline Material min-size coverage (`TextButton` default `64x40`), style-driven constraint coverage for `minimumSize`/`maximumSize`/`fixedSize` (including finite-axis tightening, infinite-axis passthrough, and `minimumSize = 0` acceptance with negative-value rejection), style-driven label alignment coverage via `ButtonStyle.Alignment` (default/theme/widget precedence), state-aware text-style coverage via `ButtonStyle.TextStyle` resolver behavior (including widget resolver-null fallback to theme in disabled state), theme-token default resolution checks for `surfaceContainerLow`/`outline`/`onSurface`, theme-level button-style override coverage from `ThemeData` (`textButtonStyle`/`elevatedButtonStyle`/`outlinedButtonStyle`) including precedence order `default -> theme -> widget -> legacy`, local inherited button-theme wrapper coverage (`TextButtonTheme`/`ElevatedButtonTheme`/`OutlinedButtonTheme`) for subtree override precedence and local null-style clearing semantics, `ThemeData` button-theme data coverage (`TextButtonTheme`/`ElevatedButtonTheme`/`OutlinedButtonTheme` properties) including precedence over legacy `*ButtonStyle` when both are set, `ButtonStyle.Merge(...)` semantics coverage (fills null fields without overriding existing values), initial `ButtonStyle` state-property override coverage (foreground/min-size/side), and expanded `StyleFrom(...)` coverage (disabled colors, text-style propagation, legacy parameter precedence, `foregroundColor`-derived overlay/splash, explicit `overlayColor` state-opacities+splash fallback, activation-captured ripple tint, interactive-state-only overlay application, transparent overlay highlight suppression, disabled-state fallback to default tokens when style resolver returns null, disabled-only override preservation of enabled-state defaults, resolver-null fallback to lower-priority/default style per state including `ElevatedButton` foreground/background and `OutlinedButton` foreground/side, direct `ButtonStyle` overlay-to-splash fallback across text/elevated/outlined buttons when `SplashColor` is omitted, and style-state parity checks mirrored for `ElevatedButton`/`OutlinedButton`). | Advanced app-bar variants/scroll-under behavior (sliver app bars, dynamic elevation, adaptive title/action spacing beyond current `titleSpacing`/text-style scope) and full Flutter `ButtonStyle` parity matrix remain pending for later M4 steps. |
| Semantics tree | `src/Flutter.Tests/SemanticsTreeTests.cs` | Actions, merge/split, transform/clip, dirty propagation and id reuse contracts. | End-to-end assistive flows with platform accessibility tooling. |
| Host semantics bridge | `src/Flutter.Tests/FlutterHostSemanticsTests.cs` | `FlutterHost` semantics runtime surface (`SemanticsRoot`, `SemanticsUpdated`, `PerformSemanticsAction`) and host-level action routing for interactive nodes. | Platform adapter wiring to native accessibility APIs on desktop/web/mobile runtimes. |
| Gestures and hit testing | `src/Flutter.Tests/GesturePipelineTests.cs` | Transform/clip hit testing, recognizer dispatch, arena conflict outcomes, pointer signals, and hover enter/exit transition dispatch (`PointerEnterEvent`/`PointerExitEvent`) from `GestureBinding` hover-hit tracking. | Multi-pointer gesture interactions and cancellation races. |
| Navigation | `src/Flutter.Tests/NavigationTests.cs` | Push/pop/replace/remove APIs, named routes, observers, route data semantics. | Host back integration across nested navigators. |
| Focus and keyboard flow | `src/Flutter.Tests/FocusTests.cs`, `src/Flutter.Tests/FlutterHostInputTests.cs` | Focus ownership transitions, primary focus bookkeeping, autofocus, key callback dispatch, host keyboard dispatch (`FlutterHost` key-down/key-up forwarding to `FocusManager`), tab traversal order/shift reversal, scope-bounded traversal boundaries, directional traversal fallback, geometry-aware directional candidate selection via traversal bounds, and transform-aware traversal-rect resolution through render-object transform chains. | Rotated/non-invertible transform edge cases and deeper scope hierarchy traversal stress cases. |
| Editable text input | `src/Flutter.Tests/TextInputTests.cs` | Focused text input delivery via host -> focus manager dispatch, controller text mutation, selection-aware insert/delete behavior, controller composing lifecycle (`SetComposing`/`CommitComposing`/`ClearComposing`), focus-manager composition dispatch (`update`/`commit`), host preedit bridge dispatch (`FlutterHost` text-input client `SetPreeditText`), host IME state sync (`SurroundingText`, selection getter/setter, cursor rectangle), multiline editing baseline (`Enter` newline insertion + `ArrowUp/ArrowDown` vertical caret movement), `TextLayout`-driven caret geometry with fallback, caret/selection navigation keys (`Left/Right/Home/End`, shift extend), word-level shortcuts (`Ctrl/Alt + ArrowLeft/ArrowRight` and `Ctrl/Alt + Backspace/Delete`), paragraph-level shortcuts in multiline mode (`Ctrl/Alt + ArrowUp/ArrowDown`), clipboard shortcuts (`Ctrl/Meta + C/X/V`), grapheme-aware caret/delete behavior for ZWJ/combining sequences, `Ctrl/Meta+A` selection, disabled-input behavior, and `onChanged` callback flow. | Advanced visual bidi navigation parity and platform text-action menu parity. |
| Scroll/slivers core | `src/Flutter.Tests/ScrollPipelineTests.cs` | Scroll position physics, viewport/sliver layout, cache extent, keep-alive reuse, sliver padding offset behavior, and `Scrollable + ListView.Separated` viewport continuity under jump-driven scroll updates. | Stress tests with very large child counts and rapid direction changes. |
| Scroll widget infrastructure | `src/Flutter.Tests/ScrollInfrastructureTests.cs` | Notifications, primary controller, keep-alive mixin, list/grid constructor semantics. | Combined nested scrollables and scrollbar interaction nuances. |
| Sample state behavior | `src/Flutter.Tests/SampleCounterStateTests.cs` | Counter model notifications and scope dependency behavior. | End-to-end sample page regressions across all demo routes. |

## How to Use for Iterative Work

1. Pick feature area row.
2. Read listed tests before opening implementation files.
3. Update or add tests in same row when behavior changes.
4. If no row fits, add one before shipping the feature.

## Porting Rule (Dart -> C#)

- For control/widget ports, tests must validate Dart-parity-critical behavior from `docs/ai/PORTING_MODE.md`:
  - defaults,
  - interaction states,
  - high-risk constraint/layout scenarios.
